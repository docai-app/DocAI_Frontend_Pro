import SimpleLayout from '../../layout/SimpleLayout';
import withLayout from '../../layout/withLayout';
import ProjectContainer from './ProjectContainer';

export default withLayout(ProjectContainer, SimpleLayout);
