import SimpleLayout from '../../../layout/SimpleLayout';
import withLayout from '../../../layout/withLayout';
import CreateContainer from './CreateContainer';

export default withLayout(CreateContainer, SimpleLayout);
